# repositorio-de-teste
testando o repositorio
//abrir o git bach here
no terminal git 
$ git config --global user.nome "seu nome"
$ git config --global user.email "seu email"
para verificar se foi feito
$ git config user.nome
$ git congig user.email
//no vscode 
abrir o terminal - git bach
$ git init
$ git status
$ git add .
$ git commit -m "coloque a mensagem que voce quiser"
//ir no git hub
criar um repositorio publico
coloque um nome parecido com o que esta fazendo
crie o repositorio
cole os comandos um por vez no vs code
//vai abrir uma guia para logar
no ser repositorio atualize a pagina 
